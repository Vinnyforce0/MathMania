// =======================
// CONFIGURAÇÃO GLOBAL
// =======================

const APP_VERSION = "1.0.6";

const versionupdate = document.getElementById('appVersion');
versionupdate.textContent = `Versão: ${APP_VERSION}`;